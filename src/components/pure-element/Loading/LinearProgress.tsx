import LinearProgress from "@mui/material/LinearProgress";

const CustomLinearProgress = () => {
  return <LinearProgress className="absolute top-0 left-0 right-0 z-50" />;
};
export default CustomLinearProgress;
